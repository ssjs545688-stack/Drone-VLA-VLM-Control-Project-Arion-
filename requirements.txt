# ============================================================
# Drone-VLA-VLM-Control-Project-Arion  -  requirements.txt
# ============================================================
# pip install -r requirements.txt
# ============================================================

# ------ 웹 서버 (smartphone_bridge) ------
fastapi
uvicorn
pydantic

# ------ LLM / 딥러닝 ------
torch
trl
datasets
transformers==5.17.0
accelerate==1.15.0
peft==0.20.0
gptqmodel==7.5.0
torchao==0.18.0
triton==3.8.0
numpy==2.2.6
scipy==1.15.3
# ------ 데이터 / 유틸리티 ------
tqdm
PyYAML                # import yaml

# ------ ROS 2 빌드 도구 (colcon 환경 밖에서 설치할 경우) ------
setuptools

# 심볼릭 링크 생성
ln -s ~/Drone-VLA-VLM-Control-Project-Arion-/world/red_target.sdf \
~/PX4-Autopilot/Tools/simulation/gz/worlds/red_target.sdf

# Vision
pip3 install opencv-python==4.10.0.84
sudo apt install gz-harmonic
sudo apt install ros-humble-ros-gzharmonic
sudo apt install ros-humble-ros-gzharmonic-bridge